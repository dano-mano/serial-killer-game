/**
 * @repo/game-engine package entry point.
 *
 * This package contains the Phaser 3 game engine code.
 * MUST NOT import React or any React-dependent libraries (Constitution Principle VII).
 * MUST NOT import Next.js internals.
 *
 * Game scenes, systems, and entities will be exported from here as they are added.
 */
